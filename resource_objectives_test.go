// Code generated by redwood. DO NOT EDIT.
package cadenya_test

import (
	"context"
	"github.com/stretchr/testify/suite"
	"testing"

	cadenya "go.cadenya.com/cadenya-go"
)

type ObjectivesSuite struct {
	suite.Suite
}

func TestObjectivesSuite(t *testing.T) {
	suite.Run(t, new(ObjectivesSuite))
}

func (s *ObjectivesSuite) TestList() {
	g := loadGolden(s.T(), "ObjectiveService_ListObjectives")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	page, err := client.Objectives().List(ctx, (&cadenya.ObjectiveListBuilder{}).
		WorkspaceID("sample").
		Limit(1).
		Cursor("sample").
		AgentID("sample").
		ParentObjectiveID("sample").
		State(cadenya.ObjectiveServiceListObjectivesState("STATE_PENDING")).
		ProfileID("sample").
		SortOrder("sample").
		IncludeInfo(true).
		AgentScheduleID("sample").
		Labels("sample").
		TenantID("sample").
		SubjectID("sample").
		WidgetID("sample").
		WidgetSessionID("sample").
		ToParams())
	s.Require().NoError(err)
	items, err := page.All(ctx)
	s.Require().NoError(err)
	s.Require().Len(items, 2)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().List(ctx, (&cadenya.ObjectiveListBuilder{}).
			Limit(1).
			Cursor("sample").
			AgentID("sample").
			ParentObjectiveID("sample").
			State(cadenya.ObjectiveServiceListObjectivesState("STATE_PENDING")).
			ProfileID("sample").
			SortOrder("sample").
			IncludeInfo(true).
			AgentScheduleID("sample").
			Labels("sample").
			TenantID("sample").
			SubjectID("sample").
			WidgetID("sample").
			WidgetSessionID("sample").
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestCreate() {
	g := loadGolden(s.T(), "ObjectiveService_CreateObjective")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Objectives().Create(ctx, (&cadenya.ObjectiveCreateBuilder{}).
		WorkspaceID("sample").
		AgentID("sample").
		VariationID("sample").
		Metadata(&cadenya.CreateOperationMetadata{}).
		SystemPromptData(map[string]any{}).
		FirstUserMessage("sample").
		Secrets(cadenya.CreateObjectiveRequest_Secret{}).
		MemoryCascade(cadenya.MemoryReference{MemoryLayerID: "sample"}).
		FirstUserMessageData(map[string]any{}).
		EpisodicMemory(&cadenya.ObjectiveEpisodicConfigParam{Key: "sample"}).
		Tenant(&cadenya.TenantAssertion{ID: "sample"}).
		Subject(&cadenya.SubjectAssertion{ID: "sample"}).
		PinnedParameters(map[string]string{}).
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().Create(ctx, (&cadenya.ObjectiveCreateBuilder{}).
			AgentID("sample").
			VariationID("sample").
			Metadata(&cadenya.CreateOperationMetadata{}).
			SystemPromptData(map[string]any{}).
			FirstUserMessage("sample").
			Secrets(cadenya.CreateObjectiveRequest_Secret{}).
			MemoryCascade(cadenya.MemoryReference{MemoryLayerID: "sample"}).
			FirstUserMessageData(map[string]any{}).
			EpisodicMemory(&cadenya.ObjectiveEpisodicConfigParam{Key: "sample"}).
			Tenant(&cadenya.TenantAssertion{ID: "sample"}).
			Subject(&cadenya.SubjectAssertion{ID: "sample"}).
			PinnedParameters(map[string]string{}).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestRetrieve() {
	g := loadGolden(s.T(), "ObjectiveService_GetObjective")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Objectives().Retrieve(ctx, "sample", (&cadenya.ObjectiveRetrieveBuilder{}).
		WorkspaceID("sample").
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().Retrieve(ctx, "sample", (&cadenya.ObjectiveRetrieveBuilder{}).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestListContextWindows() {
	g := loadGolden(s.T(), "ObjectiveService_ListObjectiveContextWindows")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	page, err := client.Objectives().ListContextWindows(ctx, "sample", (&cadenya.ObjectiveListContextWindowsBuilder{}).
		WorkspaceID("sample").
		Limit(1).
		Cursor("sample").
		IncludeInfo(true).
		Labels("sample").
		ToParams())
	s.Require().NoError(err)
	items, err := page.All(ctx)
	s.Require().NoError(err)
	s.Require().Len(items, 2)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/context_windows", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().ListContextWindows(ctx, "sample", (&cadenya.ObjectiveListContextWindowsBuilder{}).
			Limit(1).
			Cursor("sample").
			IncludeInfo(true).
			Labels("sample").
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestRetrieveDiagnostics() {
	g := loadGolden(s.T(), "ObjectiveService_GetObjectiveDiagnostics")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Objectives().RetrieveDiagnostics(ctx, "sample", (&cadenya.ObjectiveRetrieveDiagnosticsBuilder{}).
		WorkspaceID("sample").
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/diagnostics", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().RetrieveDiagnostics(ctx, "sample", (&cadenya.ObjectiveRetrieveDiagnosticsBuilder{}).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestListEvents() {
	g := loadGolden(s.T(), "ObjectiveService_ListObjectiveEvents")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	page, err := client.Objectives().ListEvents(ctx, "sample", (&cadenya.ObjectiveListEventsBuilder{}).
		WorkspaceID("sample").
		Limit(1).
		Cursor("sample").
		SortOrder("sample").
		IncludeInfo(true).
		WindowID("sample").
		SinceEventID("sample").
		Labels("sample").
		ToParams())
	s.Require().NoError(err)
	items, err := page.All(ctx)
	s.Require().NoError(err)
	s.Require().Len(items, 2)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/events", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().ListEvents(ctx, "sample", (&cadenya.ObjectiveListEventsBuilder{}).
			Limit(1).
			Cursor("sample").
			SortOrder("sample").
			IncludeInfo(true).
			WindowID("sample").
			SinceEventID("sample").
			Labels("sample").
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestStreamEvents() {
	g := loadGolden(s.T(), "ObjectiveEventStreamsService_StreamObjectiveEvents")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	stream, err := client.Objectives().StreamEvents(ctx, "sample", (&cadenya.ObjectiveStreamEventsBuilder{}).
		WorkspaceID("sample").
		ToParams())
	s.Require().NoError(err)
	count := 0
	for stream.Next() {
		count++
	}
	s.Require().NoError(stream.Err())
	s.Require().Equal(2, count)
	s.Require().Equal("e2", stream.LastEventID())
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/events:stream", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		stream, err := client.Objectives().StreamEvents(ctx, "sample", (&cadenya.ObjectiveStreamEventsBuilder{}).
			ToParams())
		s.Require().NoError(err)
		for stream.Next() {
		}
		s.Require().NoError(stream.Err())
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestListFeedback() {
	g := loadGolden(s.T(), "ObjectiveService_ListObjectiveFeedback")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	page, err := client.Objectives().ListFeedback(ctx, "sample", (&cadenya.ObjectiveListFeedbackBuilder{}).
		WorkspaceID("sample").
		Limit(1).
		Cursor("sample").
		Labels("sample").
		ToParams())
	s.Require().NoError(err)
	items, err := page.All(ctx)
	s.Require().NoError(err)
	s.Require().Len(items, 2)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/feedback", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().ListFeedback(ctx, "sample", (&cadenya.ObjectiveListFeedbackBuilder{}).
			Limit(1).
			Cursor("sample").
			Labels("sample").
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestCreateFeedback() {
	g := loadGolden(s.T(), "ObjectiveService_CreateObjectiveFeedback")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Objectives().CreateFeedback(ctx, "sample", (&cadenya.ObjectiveCreateFeedbackBuilder{}).
		WorkspaceID("sample").
		Metadata(&cadenya.CreateOperationMetadata{}).
		Data(&cadenya.ObjectiveFeedbackData{}).
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/feedback", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().CreateFeedback(ctx, "sample", (&cadenya.ObjectiveCreateFeedbackBuilder{}).
			Metadata(&cadenya.CreateOperationMetadata{}).
			Data(&cadenya.ObjectiveFeedbackData{}).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestListTasks() {
	g := loadGolden(s.T(), "ObjectiveService_ListObjectiveTasks")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	page, err := client.Objectives().ListTasks(ctx, "sample", (&cadenya.ObjectiveListTasksBuilder{}).
		WorkspaceID("sample").
		Limit(1).
		Cursor("sample").
		SortOrder("sample").
		ToParams())
	s.Require().NoError(err)
	items, err := page.All(ctx)
	s.Require().NoError(err)
	s.Require().Len(items, 2)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/tasks", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().ListTasks(ctx, "sample", (&cadenya.ObjectiveListTasksBuilder{}).
			Limit(1).
			Cursor("sample").
			SortOrder("sample").
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestRetrieveTask() {
	g := loadGolden(s.T(), "ObjectiveService_GetObjectiveTask")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Objectives().RetrieveTask(ctx, "sample", "sample", (&cadenya.ObjectiveRetrieveTaskBuilder{}).
		WorkspaceID("sample").
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/tasks/sample", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().RetrieveTask(ctx, "sample", "sample", (&cadenya.ObjectiveRetrieveTaskBuilder{}).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestListToolCalls() {
	g := loadGolden(s.T(), "ObjectiveService_ListObjectiveToolCalls")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	page, err := client.Objectives().ListToolCalls(ctx, "sample", (&cadenya.ObjectiveListToolCallsBuilder{}).
		WorkspaceID("sample").
		Limit(1).
		Cursor("sample").
		Status(cadenya.ObjectiveServiceListObjectiveToolCallsStatus("TOOL_CALL_STATUS_AUTO_APPROVED")).
		IncludeInfo(true).
		ExecutionStatus(cadenya.ObjectiveServiceListObjectiveToolCallsExecutionStatus("TOOL_CALL_EXECUTION_STATUS_PENDING")).
		Labels("sample").
		ToParams())
	s.Require().NoError(err)
	items, err := page.All(ctx)
	s.Require().NoError(err)
	s.Require().Len(items, 2)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/tool_calls", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().ListToolCalls(ctx, "sample", (&cadenya.ObjectiveListToolCallsBuilder{}).
			Limit(1).
			Cursor("sample").
			Status(cadenya.ObjectiveServiceListObjectiveToolCallsStatus("TOOL_CALL_STATUS_AUTO_APPROVED")).
			IncludeInfo(true).
			ExecutionStatus(cadenya.ObjectiveServiceListObjectiveToolCallsExecutionStatus("TOOL_CALL_EXECUTION_STATUS_PENDING")).
			Labels("sample").
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestRetrieveToolCall() {
	g := loadGolden(s.T(), "ObjectiveService_GetObjectiveToolCall")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Objectives().RetrieveToolCall(ctx, "sample", "sample", (&cadenya.ObjectiveRetrieveToolCallBuilder{}).
		WorkspaceID("sample").
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/tool_calls/sample", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().RetrieveToolCall(ctx, "sample", "sample", (&cadenya.ObjectiveRetrieveToolCallBuilder{}).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestApproveToolCall() {
	g := loadGolden(s.T(), "ObjectiveService_ApproveToolCall")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Objectives().ApproveToolCall(ctx, "sample", "sample", (&cadenya.ObjectiveApproveToolCallBuilder{}).
		WorkspaceID("sample").
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/tool_calls/sample:approve", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().ApproveToolCall(ctx, "sample", "sample", (&cadenya.ObjectiveApproveToolCallBuilder{}).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestDenyToolCall() {
	g := loadGolden(s.T(), "ObjectiveService_DenyToolCall")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Objectives().DenyToolCall(ctx, "sample", "sample", (&cadenya.ObjectiveDenyToolCallBuilder{}).
		WorkspaceID("sample").
		Memo("sample").
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/tool_calls/sample:deny", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().DenyToolCall(ctx, "sample", "sample", (&cadenya.ObjectiveDenyToolCallBuilder{}).
			Memo("sample").
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestSetToolCallContent() {
	g := loadGolden(s.T(), "ObjectiveService_SetToolCallContent")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Objectives().SetToolCallContent(ctx, "sample", "sample", (&cadenya.ObjectiveSetToolCallContentBuilder{}).
		WorkspaceID("sample").
		Content(cadenya.SetToolCallContentRequest_ContentBlock{Text: &cadenya.SetToolCallContentRequest_ContentBlock_Text{Type: "text", Text: &cadenya.SetToolCallContentRequest_TextBlock{Text: "sample"}}}).
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/tool_calls/sample:setContent", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().SetToolCallContent(ctx, "sample", "sample", (&cadenya.ObjectiveSetToolCallContentBuilder{}).
			Content(cadenya.SetToolCallContentRequest_ContentBlock{Text: &cadenya.SetToolCallContentRequest_ContentBlock_Text{Type: "text", Text: &cadenya.SetToolCallContentRequest_TextBlock{Text: "sample"}}}).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestListTools() {
	g := loadGolden(s.T(), "ObjectiveService_ListObjectiveTools")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	page, err := client.Objectives().ListTools(ctx, "sample", (&cadenya.ObjectiveListToolsBuilder{}).
		WorkspaceID("sample").
		Limit(1).
		Cursor("sample").
		ToParams())
	s.Require().NoError(err)
	items, err := page.All(ctx)
	s.Require().NoError(err)
	s.Require().Len(items, 2)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample/tools", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().ListTools(ctx, "sample", (&cadenya.ObjectiveListToolsBuilder{}).
			Limit(1).
			Cursor("sample").
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestCancel() {
	g := loadGolden(s.T(), "ObjectiveService_CancelObjective")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Objectives().Cancel(ctx, "sample", (&cadenya.ObjectiveCancelBuilder{}).
		WorkspaceID("sample").
		Reason("sample").
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample:cancel", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().Cancel(ctx, "sample", (&cadenya.ObjectiveCancelBuilder{}).
			Reason("sample").
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestCompact() {
	g := loadGolden(s.T(), "ObjectiveService_CompactObjective")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Objectives().Compact(ctx, "sample", (&cadenya.ObjectiveCompactBuilder{}).
		WorkspaceID("sample").
		CompactionConfig(&cadenya.AgentVariationSpec_CompactionConfig{}).
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample:compact", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().Compact(ctx, "sample", (&cadenya.ObjectiveCompactBuilder{}).
			CompactionConfig(&cadenya.AgentVariationSpec_CompactionConfig{}).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *ObjectivesSuite) TestContinue() {
	g := loadGolden(s.T(), "ObjectiveService_ContinueObjective")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Objectives().Continue(ctx, "sample", (&cadenya.ObjectiveContinueBuilder{}).
		WorkspaceID("sample").
		Message("sample").
		Enqueue(true).
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/objectives/sample:continue", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Objectives().Continue(ctx, "sample", (&cadenya.ObjectiveContinueBuilder{}).
			Message("sample").
			Enqueue(true).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}
