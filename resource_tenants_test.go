// Code generated by redwood. DO NOT EDIT.
package cadenya_test

import (
	"context"
	"github.com/stretchr/testify/suite"
	"testing"

	cadenya "go.cadenya.com/cadenya-go"
)

type TenantsSuite struct {
	suite.Suite
}

func TestTenantsSuite(t *testing.T) {
	suite.Run(t, new(TenantsSuite))
}

func (s *TenantsSuite) TestList() {
	g := loadGolden(s.T(), "TenantService_ListTenants")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	page, err := client.Tenants().List(ctx, (&cadenya.TenantListBuilder{}).
		WorkspaceID("sample").
		Limit(1).
		Cursor("sample").
		Query("sample").
		Labels("sample").
		SortOrder("sample").
		IncludeInfo(true).
		ToParams())
	s.Require().NoError(err)
	items, err := page.All(ctx)
	s.Require().NoError(err)
	s.Require().Len(items, 2)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/tenants", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Tenants().List(ctx, (&cadenya.TenantListBuilder{}).
			Limit(1).
			Cursor("sample").
			Query("sample").
			Labels("sample").
			SortOrder("sample").
			IncludeInfo(true).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *TenantsSuite) TestRetrieve() {
	g := loadGolden(s.T(), "TenantService_GetTenant")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Tenants().Retrieve(ctx, "sample", (&cadenya.TenantRetrieveBuilder{}).
		WorkspaceID("sample").
		IncludeInfo(true).
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/tenants/sample", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Tenants().Retrieve(ctx, "sample", (&cadenya.TenantRetrieveBuilder{}).
			IncludeInfo(true).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *TenantsSuite) TestDelete() {
	g := loadGolden(s.T(), "TenantService_DeleteTenant")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	result, err := client.Tenants().Delete(ctx, "sample", (&cadenya.TenantDeleteBuilder{}).
		WorkspaceID("sample").
		ToParams())
	s.Require().NoError(err)
	s.Require().NotNil(result)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/tenants/sample", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Tenants().Delete(ctx, "sample", (&cadenya.TenantDeleteBuilder{}).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}

func (s *TenantsSuite) TestListSubjects() {
	g := loadGolden(s.T(), "TenantService_ListTenantSubjects")
	server, served := goldenServer(s.T(), g)
	client := newTestClient(s.T(), server.URL)
	ctx := context.Background()
	page, err := client.Tenants().ListSubjects(ctx, "sample", (&cadenya.TenantListSubjectsBuilder{}).
		WorkspaceID("sample").
		Limit(1).
		Cursor("sample").
		Query("sample").
		SortOrder("sample").
		IncludeInfo(true).
		ToParams())
	s.Require().NoError(err)
	items, err := page.All(ctx)
	s.Require().NoError(err)
	s.Require().Len(items, 2)
	s.Require().Equal(int32(len(g.Interactions)), served.Load(), "request count")

	s.Run("workspace_id falls back to the client default", func() {
		server, hits := pathServer(s.T(), "/v1/workspaces/default_workspace_id/tenants/sample/subjects", g.Interactions[0].Response)
		client := newTestClient(s.T(), server.URL)
		ctx := context.Background()
		_, err := client.Tenants().ListSubjects(ctx, "sample", (&cadenya.TenantListSubjectsBuilder{}).
			Limit(1).
			Cursor("sample").
			Query("sample").
			SortOrder("sample").
			IncludeInfo(true).
			ToParams())
		s.Require().NoError(err)
		s.Require().Equal(int32(1), hits.Load())
	})
}
